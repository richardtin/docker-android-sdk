# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Create and publish a Docker image

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to the Container registry
        uses: docker/login-action@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push base image
        uses: docker/build-push-action@v2
        with:
          context: base
          file: base/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:base
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:base
          cache-to: type=inline

      - name: Build and push api-32 image
        uses: docker/build-push-action@v2
        with:
          context: api-32
          file: api-32/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-32
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-32
          cache-to: type=inline

      - name: Build and push api-32-ndk-r23b image
        uses: docker/build-push-action@v2
        with:
          context: api-32-ndk-r23b
          file: api-32-ndk-r23b/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-32-ndk-r23b
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-32-ndk-r23b
          cache-to: type=inline

      - name: Build and push api-32-ndk-r23c image
        uses: docker/build-push-action@v2
        with:
          context: api-32-ndk-r23c
          file: api-32-ndk-r23c/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-32-ndk-r23c
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-32-ndk-r23c
          cache-to: type=inline

      - name: Build and push api-33 image
        uses: docker/build-push-action@v2
        with:
          context: api-33
          file: api-33/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-33
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-33
          cache-to: type=inline

      - name: Build and push api-33-ndk-r24a image
        uses: docker/build-push-action@v2
        with:
          context: api-33-ndk-r24a
          file: api-33-ndk-r24a/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-33-ndk-r24a
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:api-33-ndk-r24a
          cache-to: type=inline
